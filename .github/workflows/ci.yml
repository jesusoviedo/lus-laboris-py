# name: CI & Build Docker

# on:
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]

# jobs:
#   test:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v3
#       - name: Set up Python
#         uses: actions/setup-python@v4
#         with:
#           python-version: '3.13'
#       - name: Install dependencies
#         run: |
#           pip install -r requirements.txt
#       - name: Run tests
#         run: |
#           pytest

#   build-and-push-docker:
#     needs: test
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v3
#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3
#       - name: Login to Google Cloud
#         uses: 'google-github-actions/auth@v2'
#         with:
#           credentials_json: '${{ secrets.GCP_SA_KEY }}'
#       - name: Configure Docker for Google Cloud
#         run: |
#           gcloud auth configure-docker
#       - name: Build and push Docker image (FastAPI)
#         run: |
#           docker build -f deploy/Dockerfile.fastapi -t gcr.io/$GCP_PROJECT_ID/fastapi-app:${{ github.sha }} .
#           docker push gcr.io/$GCP_PROJECT_ID/fastapi-app:${{ github.sha }}

#   deploy-cloud-run:
#     needs: build-and-push-docker
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v3
#       - name: Login to Google Cloud
#         uses: 'google-github-actions/auth@v2'
#         with:
#           credentials_json: '${{ secrets.GCP_SA_KEY }}'
#       - name: Set up gcloud CLI
#         uses: google-github-actions/setup-gcloud@v2
#         with:
#           project_id: ${{ secrets.GCP_PROJECT_ID }}
#       - name: Deploy to Cloud Run
#         run: |
#           gcloud run deploy fastapi-app \
#             --image gcr.io/$GCP_PROJECT_ID/fastapi-app:${{ github.sha }} \
#             --platform managed \
#             --region us-central1 \
#             --allow-unauthenticated
