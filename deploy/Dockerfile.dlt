# Dockerfile para proceso batch con dlt en Google Cloud Run
FROM python:3.13-slim
WORKDIR /app
COPY . .
RUN pip install --no-cache-dir -r requirements.txt
CMD ["python", "src/processing/batch_dlt.py"]
